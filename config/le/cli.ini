# Because we are using logrotate for greater flexibility, disable the internal certbot logrotation.
max-log-backups = 0

# Options used for automated renewal
authenticator = webroot
webroot-path = /var/www/html
